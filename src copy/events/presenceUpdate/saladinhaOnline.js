module.exports = async (client, oldPresence, newPresence) => {
	/*let member = newPresence.member;
	if (member.id === process.env.SALADINHA_ID) {
		if (newPresence.status === 'online') {
			let channel = member.guild.channels.cache.get(process.env.GENERAL_CHAT_ID);
			if (channel)
				channel.send(`${member.guild.members.cache.get(process.env.MY_ID).user}, Salada Lovers is ON!!`);
		}
	}*/
};
